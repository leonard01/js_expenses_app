# Connection string for default database (the one you connect to before creating the target DB)
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres

# The name of the target database to create/use
TARGET_DB=expense_tracker

# Connection string for the target database 
DATABASE_URL_FOR_DB=postgres://postgres:password@localhost:5432/expense_tracker

# The port your Express server will listen on
PORT=3000
